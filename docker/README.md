# docker/

Reserved for shared infrastructure config not owned by a single service — e.g. a future reverse proxy config or Alembic migration container. Per-service Dockerfiles live in `backend/` and `frontend/` themselves.
