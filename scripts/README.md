# scripts/

Reserved for helper/automation scripts (e.g. DB reset). The Phase 2 log generator lives in
`backend/app/simulation/` instead of here, since it's driven through the FastAPI simulation
API (`/api/simulation/*`) rather than run as a standalone script.
